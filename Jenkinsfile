pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                echo "checkout code from git" 
            }
         }
        stage('Test') {
            steps {
                echo "code testing has started"
                echo "code testing finished"
            }
        }
      }
    }
